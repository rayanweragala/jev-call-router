; Plain Asterisk: /etc/asterisk/http.conf
; Keep ARI on localhost or a private network. Use tlsenable/tlsbindaddr if the router runs on another host.

[general]
enabled = yes
bindaddr = 127.0.0.1
bindport = 8088
